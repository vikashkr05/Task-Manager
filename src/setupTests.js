import '@testing-library/jest-dom';
// react-i18next and i18next are fully mocked via __mocks__/ — no runtime init needed here.
// The mocks load en.json directly so all t() calls return real English strings in tests.
